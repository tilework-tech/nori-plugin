{
  "$comment": "Dependencies required by the nori-skillsets CLI at runtime. Versions are pulled from the main package.json during build.",
  "dependencies": [
    "ajv",
    "ajv-formats",
    "commander",
    "firebase",
    "glob",
    "open",
    "semver",
    "tar",
    "winston"
  ]
}
